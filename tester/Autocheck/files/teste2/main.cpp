#include "Serie.h"
#include "Eixo.h"
#include "Grafico.h"
#include "Tela.h"
#include "IHC.h"
#include "InterfaceSerial.h"

#include <iostream>

using namespace std;

int main(){

    // string COMM;

    // InterfaceSerial *is = new InterfaceSerial();
    // is->inicializar (COMM);

    // Grafico* g = new Grafico();
    // g->setTela(new Tela());

    // Serie *serieNasAbscissas = new Serie;
    // Serie *serieNasOrdenadas = new Serie;

    // string nameX, nameY, tituloX, tituloY, unidadeX, unidadeY, canalX, canalY;
    // double minimoX, minimoY, maximoX, maximoY;

    // cout << "Serie no eixo x: " << endl;
    // cin >> nameX;
    // serieNasAbscissas->setNome(nameX);

    // cout << "Qual o canal a ser usado: " << endl;
    // cin >> canalX;

    // is->registrar(canalX, serieNasAbscissas);

    // cout << "Titulo do eixo x: " << endl;
    // cin >>  tituloX;
    // cout << "Unidade do eixo x: " << endl;
    // cin >> unidadeX;
    // cout << "Escala minima do eixo x: " << endl;
    // cin >> minimoX;
    // cout << "Escala maxima do eixo x: " << endl;
    // cin >> maximoX;

    // g->setEixoDasAbscissas(tituloX, unidadeX, minimoX, maximoX);

    // cout << "Serie no eixo y: " << endl;
    // cin >> nameY;
    // serieNasOrdenadas->setNome(nameY);

    // cout << "Qual o canal a ser usado: " << endl;
    // cin >> canalY;

    // is->registrar(canalY, serieNasOrdenadas);

    // cout << "Titulo do eixo y: " << endl;
    // cin >> tituloY;
    // cout << "Unidade do eixo y: " << endl;
    // cin >> unidadeY;
    // cout << "Escala minima do eixo y: " << endl;
    // cin >> minimoY;
    // cout << "Escala maxima do eixo y: " << endl;
    // cin >> maximoY;

    // g->setEixoDasOrdenadas(tituloY, unidadeY, minimoY, maximoY);

    // g->setSerieNasAbscissas(serieNasAbscissas);
    // g->setSerieNasOrdenadas(serieNasOrdenadas);

    // IHC* ihc = new IHC();
    // while(!ihc->escFoiPressionado()){
    //     is->atualizar();
    //     g->desenhar();
    //     ihc->sleep();
    // }

    return 0;
}
